Initiates the git repository
    git init
Add the files to the repository
    git add .
Commit the files
    git commit -m "Commit Message"
Create a branch
    git branch -M main
Add the repo to the remote
    git remote add origin https://github.com/Kernelogy/class_workouts_v1.git
Finllay push the files to the repo
    git push -u origin main

    
git add .
git commit -m "Commit Message"
git push

